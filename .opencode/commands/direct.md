---
description: Use direct execution for a clear low-risk task without phase templates.
---

Switch execution mode to `DIRECT` using `system/00-system.md` `## Execution modes`.
Use `[MODE: DIRECT]` instead of a phase header. Act only when the request is
clear and low risk; otherwise explain why `STRUCTURED` is safer and ask for
confirmation or a mode switch. Inspect the final diff and run relevant checks.
