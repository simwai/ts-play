# CLAUDE.md

@AGENTS.md

If the import above did not expand in your client, read `AGENTS.md` at the
repo root and follow it as the agent entry point, then load
`system/bootstrap.txt` per its startup sequence.
