# Codex project config for the Baba prompt system.
# Inert for other agents (Claude Code, Hermes, Cursor, opencode) - only Codex
# CLI reads this directory. Mirrors the tier-1 MCP ladder in AGENTS.md.

# Safe interactive defaults: sandbox writes stay inside the workspace and
# approvals ask before acting. Network stays off unless a task enables it.
sandbox_mode = "workspace-write"
approval_policy = "on-request"

# Tier 1 - no keys required, same servers as the AGENTS.md fallback ladder.
# Context7 serves library docs over HTTP; pinned servers match opencode.jsonc
# so the agents stay in parity.

# GENERATED by generate-adapters.ps1 - edit opencode.jsonc, not this section.
# Codex CLI configuration for Baba prompt system

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = true

# Tier-1 MCP servers (no keys required)
[mcp_servers.context7]
url = "https://mcp.context7.com/mcp"

[mcp_servers.playwright]
command = "npx"
args = ["-y", "@playwright/mcp@0.0.80"]